<p>The selected animal is:</p>

<table>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Date Admitted</th>
    <th>Healthy?</th>
    <th>Trained?</th>
    <th>Adoptable?</th>
    <th>Adopted?</th>
    <th>Adopted by </th>
  </tr>
  <tr>
    <td><%= @animal.name %></td>
    <td><%= @animal.display_type %></td>
    <td><%= @animal.date_admitted %></td>
    <td><%= @animal.healthy %></td>
    <td><%= @animal.trained %></td>
    <td><%= @animal.adoptable? %></td>
    <td><%= @animal.adopted? %></td>
    <td><% if @animal.adopted? %>
    <%= @animal.customer.full_name %>
    <% end %></td>
  </tr>
</table>
<br>

<p>To edit this animal, click here:</p>
<a href="/animals/<%= @animal.id %>/edit">Edit</a>

<p>To delete this animal, click here:</p>
<form action="/animals/<%= @animal.id %>/delete" method="post">
  <input type="submit" value="Delete">
</form>

<p>These customers are looking for a cat exactly like <%=@animal.name %>: </p>
<table>
  <tr>
    <th>Name</th>
    <th>link</th>
  </tr>
  <% @animal.customer_type.each do |customer| %>
  <tr>
    <td><%= customer.full_name %></td>
    <td><a href="/customers/<%= customer.id %>">link</a></td>
  </tr>
  <% end %>
</table>
<br>
<p>Potential Customers who are interested in cats like <%=@animal.name %> are : </p>
<table>
  <tr>
    <th>Name</th>
    <th>link</th>
  </tr>
  <% @animal.customer_species.each do |customer| %>
  <tr>
    <td><%= customer.full_name %></td>
    <td><a href="/customers/<%= customer.id %>">link</a></td>
  </tr>
  <% end %>
</table>
