<p> The <%= @status_text %>  Are: </p>

<table>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Date Admitted</th>
    <th>Healthy?</th>
    <th>Trained?</th>
    <th>Adoptable?</th>
    <th>Link to Animal</th>
  </tr>
  <% @animals.each do |animal| %>
  <tr>
    <td><%= animal.name %></td>
    <td><%= animal.display_type %></td>
    <td><%= animal.date_admitted %></td>
    <td><%= animal.healthy %></td>
    <td><%= animal.trained %></td>
    <td><%= animal.adoptable? %></td>
    <td><a href="/animals/<%= animal.id %>">link</a></td>
  </tr>
  <% end %>
</table>

<p>You can change an animals status by clicking on it's link and editing it</p>

<br>
<br>
<table>
  <tr>
    <th><a href="/animals/status/in_shelter">Currently in Shelter</a></th>
    <th><a href="/animals/status/adoptable">Adoptable Animals</a></th>
    <th><a href="/animals/status/untrained">Untrained Animals </a></th>
    <th><a href="/animals/status/unhealthy">Unwell Animals</a></th>
    <th><a href="/animals/">Every animal, ever</a></th>
  </tr>
  <tr>
    <td> <%= Animal.in_shelter.count %></td>
    <td> <%= Animal.adoptable_number %></td>
    <td> <%= Animal.untrained.count %></td>
    <td> <%= Animal.unhealthy.count %></td>
    <td> <%= Animal.all.count %> </td>
  </tr>
</table>
<br><br><br><br>
